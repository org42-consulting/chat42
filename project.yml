name: Chat42
options:
  bundleIdPrefix: com.chat42
  deploymentTarget:
    macOS: "14.0"
  xcodeVersion: "15.0"
  generateEmptyDirectories: true
  developmentLanguage: en

packages:
  mlx-swift-examples:
    url: https://github.com/ml-explore/mlx-swift-examples
    from: "2.29.1"

targets:
  Chat42:
    type: application
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - path: Chat42/Sources
        includes:
          - "**/*.swift"
      - path: Chat42/Resources
        includes:
          - "Info.plist"
          - "Assets.xcassets"
          # The Liquid Glass icon. Xcode uses this in place of the AppIcon in
          # Assets.xcassets, which stays for build.sh's actool-less fallback.
          - "AppIcon.icon"
          - "PrivacyInfo.xcprivacy"
          - "en.lproj/Localizable.strings"
          - "nl.lproj/Localizable.strings"
    # Deliberately no `info:` block. Declaring one makes XcodeGen *generate*
    # Chat42/Resources/Info.plist from the properties listed here, overwriting the
    # real file — which silently dropped NSServices ("Ask Chat42") and reset the
    # version to 1.0. The plist is hand-maintained and is the single source of
    # truth for bundle metadata; point at it instead of regenerating it.
    entitlements:
      path: Chat42/Chat42.entitlements
    settings:
      base:
        INFOPLIST_FILE: Chat42/Resources/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.chat42.Chat42
        PRODUCT_NAME: Chat42
        SWIFT_VERSION: "5.9"
        MACOSX_DEPLOYMENT_TARGET: "14.0"
        CODE_SIGN_STYLE: Automatic
        # Must stay in step with Package.swift's swiftSettings, or `swift build`
        # and Xcode disagree about what compiles.
        SWIFT_STRICT_CONCURRENCY: complete
        ENABLE_HARDENED_RUNTIME: true
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    dependencies:
      - package: mlx-swift-examples
        product: MLXLLM
      - package: mlx-swift-examples
        product: MLXLMCommon

  Chat42Tests:
    type: bundle.unit-test
    platform: macOS
    deploymentTarget: "14.0"
    sources:
      - path: Tests/Chat42Tests
    settings:
      base:
        SWIFT_VERSION: "5.9"
        SWIFT_STRICT_CONCURRENCY: complete
        GENERATE_INFOPLIST_FILE: true
    dependencies:
      - target: Chat42

schemes:
  Chat42:
    build:
      targets:
        Chat42: all
        Chat42Tests: [test]
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - Chat42Tests
    archive:
      config: Release
